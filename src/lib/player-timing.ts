/** Verified overlay dwell time after mint succeeds (ms). Animations finish ~2.05s. */
export const VERIFIED_OVERLAY_MS = 4000;

/** Pause before auto-advancing after a failed mint (ms). */
export const MINT_FAILED_GAP_MS = 2000;

/** @deprecated Use VERIFIED_OVERLAY_MS */
export const MINT_GAP_MS = VERIFIED_OVERLAY_MS;
